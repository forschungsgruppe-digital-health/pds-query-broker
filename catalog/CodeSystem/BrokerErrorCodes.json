{
  "resourceType": "CodeSystem",
  "status": "active",
  "content": "complete",
  "name": "BrokerErrorCodes",
  "id": "broker-error-codes",
  "title": "Query Broker Error Codes",
  "description": "Machine-readable error codes of the Query Broker protocol, used in\n`OperationOutcome.issue.details`. They refine the generic FHIR\n`issue.code` so that consumers (e.g. the BFF) can react programmatically.",
  "url": "https://querybroker.example.org/fhir/CodeSystem/BrokerErrorCodes",
  "concept": [
    {
      "code": "timeout",
      "display": "PDS response timeout",
      "definition": "A PDS did not respond within the configured time window; the aggregated result is potentially incomplete. Corresponds to issue.code 'timeout'."
    },
    {
      "code": "unsupported-operation",
      "display": "Operation not supported",
      "definition": "The addressed PDS does not support the requested operation (not listed in its CapabilityStatement). Corresponds to issue.code 'not-supported'."
    },
    {
      "code": "no-capable-pds",
      "display": "No capable PDS",
      "definition": "No PDS site declared the requested operation; the broker cannot deliver any result. Corresponds to issue.code 'not-found'."
    },
    {
      "code": "pds-error",
      "display": "PDS-side processing error",
      "definition": "The connector or the local data system failed while processing the request. Corresponds to issue.code 'exception'."
    },
    {
      "code": "validation-error",
      "display": "Profile validation failed",
      "definition": "A produced resource did not conform to the configured targetProfile and was rejected before sending. Corresponds to issue.code 'invalid'."
    }
  ],
  "version": "0.1.0",
  "caseSensitive": true,
  "count": 5
}
