{
  "name": "qb-transport-stub",
  "version": "0.1.0",
  "description": "AsyncAPI Generator template: emits the Query Broker transport stubs (Python + Java) from the AMQP spec. Local template for the official @asyncapi/cli — the official language templates do not support AsyncAPI v3 yet (java-spring-template#308, python-paho-template#189).",
  "generator": {
    "renderer": "react",
    "apiVersion": "v3",
    "generator": ">=1.15.0"
  },
  "dependencies": {
    "@asyncapi/generator-react-sdk": "^1.1.2"
  }
}
